---
phase: 01-isr-safety-boot-resilience
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/drawOled.h
  - src/main.cpp
autonomous: true
requirements: [REL-03, OBS-01]

must_haves:
  truths:
    - "drawBootError(const char* reason) existe em drawOled.h"
    - "Loop de auth em setup() tem máximo de 30 tentativas antes de chamar drawBootError()"
    - "Chamadas ESP.restart() silenciosas em setup() (WiFiManager falhou, portal timeout) exibem drawBootError() antes"
    - "O motivo do erro é visível no OLED antes do restart — não é tela genérica de reset"
    - "Firmware compila sem erros com pio run"
  artifacts:
    - path: "src/drawOled.h"
      provides: "função drawBootError(const char* reason)"
      contains: "drawBootError"
    - path: "src/main.cpp"
      provides: "auth retry loop com contador + drawBootError em todos os failure paths de setup()"
      contains: "authRetryCount"
  key_links:
    - from: "setup() auth loop em main.cpp"
      to: "drawBootError() em drawOled.h"
      via: "chamada direta ao atingir authRetryCount >= 30"
      pattern: "drawBootError"
    - from: "setup() WiFiManager failure paths em main.cpp"
      to: "drawBootError() em drawOled.h"
      via: "substituição de ESP.restart() silencioso"
      pattern: "drawBootError.*WiFi"
---

<objective>
Criar a função de tela de erro de boot (drawBootError) e aplicá-la em todos os pontos de falha silenciosa no setup() — Firebase auth timeout e falhas do WiFiManager — tornando cada restart observável pelo usuário no OLED.

Purpose: Um dispositivo que reinicia sem mostrar o motivo é impossível de depurar em campo. Esta fase elimina todos os restarts silenciosos do boot.
Output: drawOled.h com drawBootError(); main.cpp com auth loop limitado e boot failures visíveis.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-isr-safety-boot-resilience/01-CONTEXT.md
@.planning/phases/01-isr-safety-boot-resilience/01-01-SUMMARY.md

<interfaces>
<!-- Código atual relevante. Executor não precisa explorar o codebase. -->

De src/drawOled.h — funções de referência para drawBootError() (linhas 327-372):

```cpp
// Modelo de assinatura a seguir (linha 327):
void drawBootProgress(const char *label, uint8_t progress)
{
  display.clear();
  display.setTextAlignment(TEXT_ALIGN_CENTER);
  display.setFont(ArialMT_Plain_16);
  display.drawString(64, 6, "Guardiao");
  display.setFont(ArialMT_Plain_10);
  display.drawString(64, 28, label);
  display.drawProgressBar(10, 44, 108, 12, progress);
  display.display();
}

// Layout visual de referência para estado de erro (linha 351):
void drawResetDevice()
{
  display.clear();
  display.setFont(ArialMT_Plain_16);
  display.setTextAlignment(TEXT_ALIGN_CENTER);
  display.drawString(64, 5, "Restarting...");
  display.drawString(64, 30, DEVICE_NAME);
  display.display();
}
```

De src/main.cpp — auth loop atual a substituir (linhas ~474-480):

```cpp
drawBootProgress("Authenticating...", 80);
Serial.println("Getting User UID");
while ((auth.token.uid) == "")
{
  Serial.print('.');
  delay(1000);
}
```

De src/main.cpp — ESP.restart() silenciosos em setup() a corrigir (linhas ~420-435):

```cpp
// Ponto 1: setupWiFiManager falhou
if (!setupWiFiManager(wifiManager))
{
  Serial.println("Failed to setup WiFi Manager");
  ESP.restart();  // ← SILENCIOSO, substituir
}

// Ponto 2: portal timeout sem configuração
if (!wifiManager.startConfigPortal(portalSSID.c_str()))
{
  Serial.println("Config portal timed out without configuration. Restarting.");
  drawResetDevice();
  delay(2000);
  ESP.restart();  // ← genérico, melhorar com drawBootError
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar drawBootError() em drawOled.h</name>
  <files>src/drawOled.h</files>
  <action>
**Em src/drawOled.h — inserir após a função drawResetDevice() (linha ~361):**

Adicionar a função nova imediatamente após `drawResetDevice()`, antes de `drawWifiNotConfigured()`:

```cpp
/**
 * Displays a named boot error on the OLED before restarting.
 * @param reason Short description of the failure (max ~20 chars for readability)
 */
void drawBootError(const char *reason)
{
  display.clear();
  display.setTextAlignment(TEXT_ALIGN_CENTER);
  display.setFont(ArialMT_Plain_16);
  display.drawString(64, 4, "Erro de Boot");
  display.setFont(ArialMT_Plain_10);
  display.drawString(64, 26, reason);
  display.drawString(64, 44, "Reiniciando...");
  display.display();
}
```

**Layout do OLED 128x64:**

- Y=4: "Erro de Boot" em fonte 16 (título, alto)
- Y=26: `reason` em fonte 10 (motivo, centro)
- Y=44: "Reiniciando..." em fonte 10 (rodapé)

**Restrições:**

- Não modificar nenhuma outra função existente neste arquivo
- Não alterar a ordem de nenhuma outra função
- Manter o mesmo estilo de comentário Doxygen das funções vizinhas
- `reason` deve ser `const char*` — não `String` (consistência com drawBootProgress)
  </action>
  <verify>
  <automated>cd /Users/manoel/repo/guardiao-project/guardiao-firmware && pio run 2>&1 | tail -5</automated>
  </verify>
  <done> - `grep -n "drawBootError" src/drawOled.h` retorna a definição da função - Função aceita `const char*` como parâmetro - `pio run` compila sem erros
  </done>
  </task>

<task type="auto">
  <name>Task 2: Auth retry limitado + boot failures visíveis em setup()</name>
  <files>src/main.cpp</files>
  <action>
**Alteração A — Auth retry loop em setup() (substituir while loop ~linha 475):**

Substituir:

```cpp
drawBootProgress("Authenticating...", 80);
Serial.println("Getting User UID");
while ((auth.token.uid) == "")
{
  Serial.print('.');
  delay(1000);
}
```

Por (per D-11, D-12, D-13, D-14 do CONTEXT.md):

```cpp
drawBootProgress("Authenticating...", 80);
Serial.println("Getting User UID");
int authRetryCount = 0;
while ((auth.token.uid) == "" && authRetryCount < 30)
{
  Serial.print('.');
  delay(1000);
  authRetryCount++;
}
if ((auth.token.uid) == "")
{
  Serial.println("\n[Auth] Firebase auth timeout after 30s. Restarting.");
  drawBootError("Firebase auth falhou");
  delay(3000);
  ESP.restart();
}
```

**Alteração B — setupWiFiManager falhou em setup() (~linha 423):**

Substituir:

```cpp
if (!setupWiFiManager(wifiManager))
{
  Serial.println("Failed to setup WiFi Manager");
  ESP.restart();
}
```

Por:

```cpp
if (!setupWiFiManager(wifiManager))
{
  Serial.println("[Boot] Failed to setup WiFi Manager");
  drawBootError("WiFi config falhou");
  delay(3000);
  ESP.restart();
}
```

**Alteração C — Config portal timeout em setup() (~linha 429):**

Substituir:

```cpp
if (!wifiManager.startConfigPortal(portalSSID.c_str()))
{
  Serial.println("Config portal timed out without configuration. Restarting.");
  drawResetDevice();
  delay(2000);
  ESP.restart();
}
```

Por:

```cpp
if (!wifiManager.startConfigPortal(portalSSID.c_str()))
{
  Serial.println("[Boot] Config portal timed out.");
  drawBootError("Portal timeout");
  delay(3000);
  ESP.restart();
}
```

**Notas de implementação:**

- `authRetryCount` é variável local — não adicionar a config.h
- `delay(3000)` antes de cada restart — tempo suficiente para o usuário ler a mensagem no OLED
- `Serial.println("\n[Auth] ...")` — o `\n` termina a linha de dots antes da mensagem de erro
- `drawBootError()` já foi criada em drawOled.h pelo plano anterior — disponível sem includes adicionais (drawOled.h já incluído no topo de main.cpp)
- Não modificar o bloco `else { initWifiManager(); }` — esse path não tem failure case
  </action>
  <verify>
  <automated>cd /Users/manoel/repo/guardiao-project/guardiao-firmware && pio run 2>&1 | tail -8</automated>
  </verify>
  <done> - `grep -n "authRetryCount" src/main.cpp` retorna as linhas de declaração e incremento - `grep -n "drawBootError" src/main.cpp` retorna 3 ocorrências (auth, WiFi config, portal timeout) - Nenhum `ESP.restart()` silencioso permanece nos failure paths de setup() — todos têm drawBootError() antes - `pio run` compila com `SUCCESS`
  </done>
  </task>

</tasks>

<verification>
```bash
# Compilação limpa
cd /Users/manoel/repo/guardiao-project/guardiao-firmware && pio run 2>&1 | grep -E "error:|SUCCESS|FAILED"

# drawBootError definida em drawOled.h

grep -n "drawBootError" src/drawOled.h

# drawBootError chamada em 3 pontos de main.cpp

grep -n "drawBootError" src/main.cpp

# Auth loop tem limite explícito

grep -n "authRetryCount" src/main.cpp

# Nenhum ESP.restart() silencioso restante nos failure paths do setup()

grep -n "ESP.restart\|ESP.reset" src/main.cpp

```
O último grep deve mostrar apenas os restarts que já têm drawBootError() ou drawResetDevice() na linha imediatamente anterior.
</verification>

<success_criteria>
- `pio run` termina com `SUCCESS` e 0 erros
- `drawBootError(const char* reason)` existe em drawOled.h com layout 3 linhas
- Auth loop em setup() limitado a 30 iterações com drawBootError() no timeout
- setupWiFiManager failure → drawBootError("WiFi config falhou") antes de restart
- startConfigPortal timeout → drawBootError("Portal timeout") antes de restart
- Todos os restarts de boot failure têm 3s de OLED antes de reiniciar
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-isr-safety-boot-resilience/01-02-SUMMARY.md` com:
- O que foi alterado e onde (número de linhas modificadas em cada arquivo)
- Resultado do `pio run` (RAM/Flash usage após as alterações)
- Lista final de todos os pontos de `drawBootError()` em main.cpp
- Status: Phase 1 completa
</output>
```
