---
phase: 03-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/drawOled.h
  - src/buzzer.h
  - src/alarm.h
  - src/buttons.h
  - src/main.cpp
autonomous: true
requirements:
  - CQ-01

must_haves:
  truths:
    - "drawOled.h contém apenas rendering: frames, overlays, modal screens, initDisplay, initUIFramework"
    - "buzzer.h contém beepConfirm, beepSuccess, beepError, beepBoot"
    - "alarm.h contém startAlarm, updateAlarm, drawAlarmScreen e estado do alarme"
    - "buttons.h contém handleButtons e estado dos botões"
    - "Build compila sem erros com a mesma RAM/Flash (~49%/67%)"
  artifacts:
    - path: "src/buzzer.h"
      provides: "Funções de feedback sonoro"
      exports: ["beepConfirm", "beepSuccess", "beepError", "beepBoot"]
    - path: "src/alarm.h"
      provides: "Sistema de alarme não-bloqueante"
      exports:
        [
          "startAlarm",
          "updateAlarm",
          "drawAlarmScreen",
          "AlarmState",
          "alarmState",
        ]
    - path: "src/buttons.h"
      provides: "Handling de botões S1/S2"
      exports: ["handleButtons"]
    - path: "src/drawOled.h"
      provides: "Rendering OLED puro — frames, overlays, modal screens"
  key_links:
    - from: "src/buttons.h"
      to: "src/buzzer.h"
      via: "beepConfirm() chamado em tap curto"
      pattern: "beepConfirm"
    - from: "src/alarm.h"
      to: "src/drawOled.h"
      via: "drawAlarmScreen() usa display object"
      pattern: "drawAlarmScreen"
    - from: "src/main.cpp"
      to: "alarm.h, buttons.h, buzzer.h"
      via: "#include na ordem correta"
      pattern: "#include"
---

<objective>
Extrair buttons.h, alarm.h, buzzer.h de drawOled.h — deixando drawOled.h como módulo puro de rendering OLED.

Purpose: drawOled.h é um monolito de 584 linhas com responsabilidades misturadas (rendering + alarme + buzzer + botões). Dificulta manutenção e causa acoplamento acidental.
Output: 3 novos headers focados + drawOled.h reduzido a ~300 linhas de rendering.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/drawOled.h
@src/main.cpp
@src/config.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar buzzer.h, alarm.h, buttons.h extraindo de drawOled.h</name>
  <files>src/buzzer.h, src/alarm.h, src/buttons.h, src/drawOled.h, src/main.cpp</files>
  <action>
**Ordem de include em main.cpp** (importa no Arduino single-TU):
```
#include <config.h>       // defines, globals
#include <icons.h>        // icon arrays
#include <drawOled.h>     // display, ui, frame funcs, modal screens (já existente)
#include <buzzer.h>       // NEW — beep funcs (usa BUZZER_PIN de config.h)
#include <alarm.h>        // NEW — alarm state + funcs (usa display de drawOled.h, tone de buzzer)
#include <buttons.h>      // NEW — handleButtons (usa ui, display, buzzer, alarm)
```

NOTA: drawOled.h CONTINUA incluindo config.h e icons.h (seus includes próprios não mudam). Os novos headers NÃO incluem nada — dependem de estarem incluídos após drawOled.h em main.cpp (padrão Arduino single-TU).

**src/buzzer.h** — Criar com `#pragma once` e extrair de drawOled.h:

- `beepConfirm()` (linha 462)
- `beepSuccess()` (linhas 464-472)
- `beepError()` (linhas 473-483)
- `beepBoot()` (linhas 484-497)
- Nenhuma variável de estado — são funções puras usando BUZZER_PIN

**src/alarm.h** — Criar com `#pragma once` e extrair de drawOled.h:

- Variáveis de estado: `alarmState`, `alarmStartMs`, `alarmToneMs`, `alarmFreq`, `alarmToneOn`
- Enum: `AlarmState { ALARM_OFF, ALARM_SOUNDING }`
- `drawAlarmScreen()` (linhas 314-326) — usa `display` definido em drawOled.h
- `startAlarm()` (linhas 407-416) — seta estado e chama drawAlarmScreen()
- `updateAlarm()` (linhas 418-460) — usa tone(), BUZZER_PIN, botões, displayMode
- MOVER `DisplayMode` enum e `displayMode` para alarm.h (alarm depende deles, drawOled.h frame funcs precisam ler displayMode mas não setam)

**Ajuste necessário**: drawOled.h atualmente define `DisplayMode` e `displayMode`. Mover para alarm.h e usar forward-reference. Como alarm.h é incluído DEPOIS de drawOled.h no TU, drawOled.h não pode depender de alarm.h. Solução: manter `DisplayMode` enum e `displayMode` em drawOled.h (eles são estado de UI) e alarm.h os usa via single-TU visibility. NÃO mover.

**src/buttons.h** — Criar com `#pragma once` e extrair de drawOled.h:

- Variáveis de estado: `s1PressStart`, `s2PressStart`, `s1Handled`, `s2Handled`
- `handleButtons()` (linhas 499-584) — usa ui, display, BUZZER_PIN, drawHoldProgress, drawResetDevice, beepConfirm, openConfigPortal

**drawOled.h** — Deletar as funções e variáveis movidas. Manter:

- Hardware objects (display, ui, oneWire) — linhas 14-17
- UI Constants e State (STATUS_BAR_H, DisplayMode, displayMode, prevTemperature, prevSensorError, lastVccMs, firebaseConnected) — linhas 20-37
- Forward declaration openConfigPortal — linha 44
- Frame callbacks (frameTemperature, frameThreshold, frameStatus, frameWifi, frameBattery, frameClock) — linhas 51-245
- overlayStatusBar() — linhas 247-289
- initDisplay(), initUIFramework() — linhas 291-326\*\*
- Modal screens (drawBootProgress, drawWifiReconnecting, drawHoldProgress, drawResetDevice, drawBootError, drawWifiNotConfigured) — linhas 328-401

**main.cpp** — Atualizar includes:

- Adicionar `#include <buzzer.h>` após `#include <drawOled.h>`
- Adicionar `#include <alarm.h>` após buzzer.h
- Adicionar `#include <buttons.h>` após alarm.h
- Remover nada (os includes existentes permanecem)
  </action>
  <verify>
  <automated>cd /Users/manoel/repo/guardiao-project/guardiao-firmware && ~/.platformio/penv/bin/pio run -e nodemcuv2 2>&1 | tail -10</automated>
  </verify>
  <done>Build SUCCESS. drawOled.h tem ~300 linhas (só rendering). buzzer.h, alarm.h, buttons.h existem e compilam. RAM/Flash próximos de 49%/67%.</done>
  </task>

<task type="auto">
  <name>Task 2: Validar separação — grep confirma que cada função está no header correto</name>
  <files>src/buzzer.h, src/alarm.h, src/buttons.h, src/drawOled.h</files>
  <action>
Executar verificações grep para confirmar a separação:

1. `grep -l "beepConfirm\|beepSuccess\|beepError\|beepBoot" src/*.h` → apenas buzzer.h
2. `grep -l "startAlarm\|updateAlarm\|ALARM_SOUNDING" src/*.h` → apenas alarm.h
3. `grep -l "handleButtons\|s1PressStart\|s2PressStart" src/*.h` → apenas buttons.h
4. `grep -l "void frame\|overlayStatusBar\|initDisplay\|initUIFramework" src/*.h` → apenas drawOled.h
5. `wc -l src/drawOled.h` → deve ser ~280-320 linhas (era 584)
6. Confirmar que drawOled.h NÃO contém: handleButtons, startAlarm, updateAlarm, beepConfirm, beepSuccess, beepError, beepBoot

Se alguma verificação falhar, ajustar e recompilar.
</action>
<verify>
<automated>cd /Users/manoel/repo/guardiao-project/guardiao-firmware && echo "buzzer:" && grep -l "beepConfirm" src/_.h && echo "alarm:" && grep -l "startAlarm" src/_.h && echo "buttons:" && grep -l "handleButtons" src/\*.h && echo "drawOled lines:" && wc -l src/drawOled.h</automated>
</verify>
<done>Cada função está exclusivamente no header correto. drawOled.h tem ~300 linhas. Build continua passando.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/manoel/repo/guardiao-project/guardiao-firmware
~/.platformio/penv/bin/pio run -e nodemcuv2 2>&1 | grep -E "error:|warning:|SUCCESS|FAILED|RAM|Flash"
wc -l src/drawOled.h src/buzzer.h src/alarm.h src/buttons.h
grep -c "handleButtons\|startAlarm\|beepConfirm" src/drawOled.h  # deve ser 0
```
</verification>

<success_criteria>

- Build compila sem erros (pio run exits 0)
- drawOled.h reduzido de 584 para ~300 linhas
- buzzer.h, alarm.h, buttons.h existem com as funções corretas
- Nenhuma duplicação de função entre headers
- RAM/Flash Usage ~49%/67% (sem aumento significativo)
  </success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-code-quality/03-01-SUMMARY.md`
</output>
