---
phase: 02-firebase-resilience-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.h
  - src/EEPROMFunction.h
  - src/main.cpp
autonomous: true
requirements:
  - REL-04
  - REL-05

must_haves:
  truths:
    - "Falha no Firebase.RTDB.updateNode() incrementa firebaseFailCount e seta firebaseSkipRemaining (backoff) em vez de resetar imediatamente"
    - "Leituras não enviadas durante backoff são salvas na EEPROM (pendingQueue) e sobrevivem a reboot manual"
    - "Após um write bem-sucedido, um pending reading é drenado da EEPROM por ciclo"
    - "getDeviceConfigurations() não é chamado em sendDataToFireBase() — tem timer próprio de 60 min em loop()"
    - "Quando firebaseFailCount >= 5 e firebaseSkipRemaining == 0, o dispositivo reseta via drawResetDevice()"
  artifacts:
    - path: "src/config.h"
      provides: "Defines PENDING_QUEUE_*, CONFIG_FETCH_INTERVAL; globals firebaseFailCount, firebaseSkipRemaining, configFetchPrevMillis, pendingQueue[], pendingQueueLen"
      contains: "PENDING_QUEUE_ADDR"
    - path: "src/EEPROMFunction.h"
      provides: "struct PendingReading, loadPendingQueue(), enqueuePendingReading(), drainOnePendingReading()"
      exports:
        ["loadPendingQueue", "enqueuePendingReading", "drainOnePendingReading"]
    - path: "src/main.cpp"
      provides: "sendDataToFireBase() com bool return + backoff; callFirebase() com skip guard; setup() com loadPendingQueue; loop() com config timer"
  key_links:
    - from: "callFirebase()"
      to: "enqueuePendingReading(temperature, timestamp)"
      via: "Skip guard: quando firebaseSkipRemaining > 0, decrementa e enfileira antes de retornar"
      pattern: "firebaseSkipRemaining > 0"
    - from: "sendDataToFireBase()"
      to: "enqueuePendingReading(temperature, timestamp)"
      via: "Retorno false do updateNode()"
      pattern: "Firebase.RTDB.updateNode.*false"
    - from: "sendDataToFireBase()"
      to: "drainOnePendingReading()"
      via: "Retorno true do updateNode()"
      pattern: "drainOnePendingReading"
    - from: "setup()"
      to: "loadPendingQueue()"
      via: "Chamada imediatamente após EEPROM.begin(EEPROM_SIZE)"
      pattern: "loadPendingQueue"
    - from: "loop()"
      to: "getDeviceConfigurations()"
      via: "Guard: millis() - configFetchPrevMillis > CONFIG_FETCH_INTERVAL"
      pattern: "configFetchPrevMillis"
---

<objective>
Construir a infraestrutura de fila persistente de leituras na EEPROM e integrar o backoff exponencial no Firebase + decoupling do config fetch.

Purpose: Eliminar o boot-loop agressivo causado por falhas temporárias no Firebase, preservar leituras durante outages e separar o fetch de configuração do ciclo de envio.
Output: config.h com defines + globals, EEPROMFunction.h com struct + 3 funções de fila, main.cpp com sendDataToFireBase() e callFirebase() refatorados + loop() com config timer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-firebase-resilience-observability/02-CONTEXT.md

<!-- Leia estes arquivos ANTES de implementar -->

@src/config.h
@src/EEPROMFunction.h
@src/main.cpp
</context>

<interfaces>
<!-- Contratos existentes que o executor precisa conhecer. -->

De src/config.h (linhas relevantes):

```cpp
const int LDID_ADDR = 300;   // após este — adicionar PENDING_QUEUE_* defines
unsigned long lastHeartbeatMillis = 0;  // padrão de nomenclatura para configFetchPrevMillis
unsigned long timerDelay = 300000L;     // contexto de timing
```

De src/EEPROMFunction.h (padrão de funções EEPROM):

```cpp
const int EEPROM_SIZE = 512; // MAX EEPROM SIZE
// Padrão: EEPROM.put(addr, data) / EEPROM.get(addr, data) para structs
// EEPROM.commit() apenas após writes reais, nunca em loop tight
// writeStringToEEPROM() chama EEPROM.commit() internamente
```

De src/main.cpp — callFirebase() atual (linhas ~337-388):

```cpp
void callFirebase() {
  prevTemperature = temperature;
  temperature = readTemperature();
  firebaseConnected = Firebase.ready();

  if (Firebase.ready() && (millis() - sendDataPrevMillis > timerDelay || sendDataPrevMillis == 0)) {
    sendDataToFireBase();
  } else if (!Firebase.ready() && ...) {
    // WiFi reconnect loop (3 tentativas), fallback ESP.reset()
  }
  // threshold checks...
}
```

De src/main.cpp — sendDataToFireBase() atual (linhas ~294-330):

```cpp
void sendDataToFireBase() {
  sendDataPrevMillis = millis();
  getDeviceConfigurations();  // <-- REMOVER daqui
  timestamp = (int)getTime();
  if (temperature != 888.0 && timestamp > MIN_VALID_TIMESTAMP) {
    // ...
    Serial.printf("Set json... %s\n",
      Firebase.RTDB.updateNode(&fbdo, databasePath.c_str(), &json) ? "ok" : fbdo.errorReason().c_str());
    // ^^ capturar este bool
    checkThresholdAlert();
  }
}
```

De src/main.cpp — setup() linha ~416:

```cpp
EEPROM.begin(EEPROM_SIZE);
// adicionar loadPendingQueue() aqui, após esta linha
USER_EMAIL = readStringFromEEPROM(EMAIL_ADDR);
```

De src/main.cpp — loop() atual (linhas ~562-590):

```cpp
void loop() {
  if (millis() - lastFirebaseCallMs >= GET_DATA_INTERVAL) {
    lastFirebaseCallMs = millis();
    callFirebase();
  }
  handleButtons();
  if (updateAlarm()) { }
  else if (displayMode == DMODE_NORMAL) { ui.update(); }
  if (millis() - lastHeartbeatMillis > HEARTBEAT_INTERVAL) { sendHeartbeat(); }
  watchDogCount = 0;
  ArduinoOTA.handle();
  yield();
}
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Infraestrutura — config.h defines + EEPROMFunction.h struct e funções de fila</name>
  <files>src/config.h, src/EEPROMFunction.h</files>
  <action>
**src/config.h** — Adicionar em 3 blocos, respeitando o padrão existente (SCREAMING_SNAKE defines, camelCase vars):

1. Após `const int LDID_ADDR = 300;` (antes do bloco de Runtime state):

```cpp
// ---- EEPROM pending queue (fila de leituras não enviadas)
#define PENDING_QUEUE_ADDR    380   // Addr 380–509 (130 bytes)
#define PENDING_QUEUE_MAGIC   0xA5  // Distingue fila válida de EEPROM virgem (0xFF)
#define PENDING_QUEUE_MAXLEN  16    // Máx 16 entries × 8 bytes = 128 bytes de dados
```

2. Após `#define HEARTBEAT_INTERVAL 300000L`:

```cpp
#define CONFIG_FETCH_INTERVAL 3600000L  // 60 min — fetch de configurações Firebase
```

3. Após `unsigned long lastHeartbeatMillis = 0;` no bloco de Timer bookkeeping:

```cpp
unsigned long configFetchPrevMillis = 0;  // Timer para CONFIG_FETCH_INTERVAL
```

4. Após `uint8_t resetReason = 1;` no bloco de Runtime counters:

```cpp
// ---- Firebase backoff state
uint8_t firebaseFailCount    = 0;  // Falhas consecutivas de write no RTDB
uint8_t firebaseSkipRemaining = 0; // Ciclos a pular antes de tentar novamente
```

5. Após os includes no topo de config.h (antes dos const int addr), adicionar forward declaration do struct (EEPROMFunction.h ainda não foi incluído aqui):

```cpp
// ---- Pending queue globals (struct definido em EEPROMFunction.h — incluído antes)
// Declarados após o include de EEPROMFunction.h em main.cpp
```

NOTA: os arrays `pendingQueue[]` e `pendingQueueLen` precisam do tipo `PendingReading` que está em EEPROMFunction.h. Como EEPROMFunction.h é incluído ANTES de config.h em main.cpp, definir os globals pendingQueue/pendingQueueLen em EEPROMFunction.h (junto com o struct e as funções), não em config.h. Isso evita dependência circular. Adicionar em EEPROMFunction.h após o struct:

```cpp
PendingReading pendingQueue[PENDING_QUEUE_MAXLEN];
uint8_t        pendingQueueLen = 0;
```

---

**src/EEPROMFunction.h** — Adicionar após o bloco de writeStringToEEPROM():

1. Struct PendingReading (8 bytes por entry):

```cpp
struct PendingReading {
  uint32_t timestamp;
  float    temperature;
}; // 8 bytes — EEPROM.put()/get() safe
```

2. Globals da fila (dependem do struct e dos defines de config.h — devem vir APÓS os #includes em main.cpp):

```cpp
PendingReading pendingQueue[PENDING_QUEUE_MAXLEN];
uint8_t        pendingQueueLen = 0;
```

ATENÇÃO: config.h é incluído em main.cpp ANTES de EEPROMFunction.h? Verificar a ordem de includes em main.cpp. Se config.h vem antes, colocar os globals em EEPROMFunction.h. Se EEPROMFunction.h vem antes, colocar em config.h. Escolher o que funcionar sem redeclaração.

Na prática: verificar main.cpp. Se a ordem de includes já existe, definir tanto o struct quanto os globals de pendingQueue[] em EEPROMFunction.h (um só local). Nos defines PENDING*QUEUE*\* o macro PENDING_QUEUE_MAXLEN deve estar disponível — garantir que config.h é incluído ANTES de EEPROMFunction.h em main.cpp. Se não, usar literal 16 no array ou reordenar includes.

3. `loadPendingQueue()` — chamada em setup() após EEPROM.begin():

```cpp
void loadPendingQueue() {
  uint8_t magic = EEPROM.read(PENDING_QUEUE_ADDR);
  if (magic == PENDING_QUEUE_MAGIC) {
    uint8_t count;
    EEPROM.get(PENDING_QUEUE_ADDR + 1, count);
    pendingQueueLen = min(count, (uint8_t)PENDING_QUEUE_MAXLEN);
    for (uint8_t i = 0; i < pendingQueueLen; i++) {
      EEPROM.get(PENDING_QUEUE_ADDR + 2 + i * sizeof(PendingReading), pendingQueue[i]);
    }
    Serial.printf("[Queue] Loaded %u pending reading(s) from EEPROM\n", pendingQueueLen);
  } else {
    pendingQueueLen = 0;
    Serial.println("[Queue] EEPROM virgin or invalid — starting empty queue");
  }
}
```

4. `enqueuePendingReading()` — chamada em sendDataToFireBase() fail e callFirebase() skip cycles:

```cpp
void enqueuePendingReading(float temp, uint32_t ts) {
  if (pendingQueueLen >= PENDING_QUEUE_MAXLEN) {
    // Ring buffer: descartar a entrada mais antiga
    Serial.printf("[Queue] Full, dropping oldest (ts=%u)\n", pendingQueue[0].timestamp);
    memmove(pendingQueue, pendingQueue + 1, (PENDING_QUEUE_MAXLEN - 1) * sizeof(PendingReading));
    pendingQueueLen = PENDING_QUEUE_MAXLEN - 1;
  }
  pendingQueue[pendingQueueLen].temperature = temp;
  pendingQueue[pendingQueueLen].timestamp   = ts;
  pendingQueueLen++;
  // Persistir na EEPROM
  EEPROM.write(PENDING_QUEUE_ADDR, PENDING_QUEUE_MAGIC);
  EEPROM.write(PENDING_QUEUE_ADDR + 1, pendingQueueLen);
  for (uint8_t i = 0; i < pendingQueueLen; i++) {
    EEPROM.put(PENDING_QUEUE_ADDR + 2 + i * sizeof(PendingReading), pendingQueue[i]);
  }
  EEPROM.commit();
  Serial.printf("[Queue] Enqueued ts=%u temp=%.2f (queue len=%u)\n", ts, temp, pendingQueueLen);
}
```

5. `drainOnePendingReading()` — chamada após write bem-sucedido:

```cpp
void drainOnePendingReading() {
  if (pendingQueueLen == 0) return;

  // Enviar a entrada mais antiga (index 0)
  FirebaseJson drainJson;
  String drainPath = "/datalogger/" + String(pendingQueue[0].timestamp);
  drainJson.set((drainPath + "/temperature").c_str(), pendingQueue[0].temperature);
  drainJson.set((drainPath + "/timestamp").c_str(),   (int)pendingQueue[0].timestamp);

  if (Firebase.RTDB.updateNode(&fbdo, databasePath.c_str(), &drainJson)) {
    // Sucesso: remover da fila
    memmove(pendingQueue, pendingQueue + 1, (pendingQueueLen - 1) * sizeof(PendingReading));
    pendingQueueLen--;
    if (pendingQueueLen == 0) {
      // Fila vazia: invalidar magic byte
      EEPROM.write(PENDING_QUEUE_ADDR, 0x00);
    } else {
      EEPROM.write(PENDING_QUEUE_ADDR + 1, pendingQueueLen);
    }
    EEPROM.commit();
    Serial.printf("[Queue] Drained 1 pending reading (queue len=%u)\n", pendingQueueLen);
  } else {
    Serial.printf("[Queue] Drain failed: %s — will retry next cycle\n", fbdo.errorReason().c_str());
  }
}
```

NOTA sobre drainOnePendingReading: usa `fbdo` e `databasePath` que são globals definidos em main.cpp. Como EEPROMFunction.h é incluído em main.cpp antes da definição dessas vars, isso pode causar erro de compilação. Resolução: declarar drainOnePendingReading() como forward-declared ou mover a chamada para main.cpp inline. Alternativa simples: tornar drainOnePendingReading() uma função void que apenas prepara os dados e chama um helper em main.cpp.

SOLUÇÃO PREFERIDA: Manter drainOnePendingReading() em EEPROMFunction.h e usar `extern FirebaseData fbdo;` e `extern String databasePath;` dentro do header (safe em Arduino framework com um único translation unit). OU: simplificar — a função apenas remove o item da RAM sem enviar para o RTDB; o envio real fica em main.cpp usando os dados removidos.

DECISÃO FINAL (per D-12): drainOnePendingReading() faz o envio. Para evitar dependência circular, usar `extern` declarations no topo de EEPROMFunction.h:

```cpp
// Forward declarations — definidos em main.cpp
extern FirebaseData fbdo;
extern String databasePath;
```

Isso é padrão em firmware Arduino com múltiplos headers.
</action>
<verify>
<automated>cd /Users/manoel/repo/guardiao-project/guardiao-firmware && pio run -e nodemcuv2 2>&1 | tail -20</automated>
</verify>
<done>Build compila sem erros e sem warnings relativos ao código novo. Funções loadPendingQueue, enqueuePendingReading, drainOnePendingReading declaradas e compilando. Defines PENDING*QUEUE*\* presentes em config.h.</done>
</task>

<task type="auto">
  <name>Task 2: Integração — main.cpp backoff + queue wire-up + config fetch timer</name>
  <files>src/main.cpp</files>
  <action>
Modificar src/main.cpp em 4 pontos:

**Ponto 1 — sendDataToFireBase(): remover getDeviceConfigurations(), capturar bool do updateNode, aplicar backoff e fila.**

Antes (estrutura atual):

```cpp
void sendDataToFireBase() {
  sendDataPrevMillis = millis();
  getDeviceConfigurations();  // REMOVER
  timestamp = (int)getTime();
  if (temperature != 888.0 && timestamp > MIN_VALID_TIMESTAMP) {
    // ... json setup ...
    Serial.printf("Set json... %s\n",
      Firebase.RTDB.updateNode(&fbdo, databasePath.c_str(), &json) ? "ok" : fbdo.errorReason().c_str());
    checkThresholdAlert();
  }
  countDataSentToFireBase += 1;
}
```

Depois:

```cpp
bool sendDataToFireBase() {
  sendDataPrevMillis = millis();
  // getDeviceConfigurations() movido para timer próprio em loop() (per D-15/D-16)
  timestamp = (int)getTime();
  if (temperature != 888.0 && timestamp > MIN_VALID_TIMESTAMP) {
    // ... json setup (sem alterações) ...
    bool writeOk = Firebase.RTDB.updateNode(&fbdo, databasePath.c_str(), &json);
    Serial.printf("Set json... %s\n", writeOk ? "ok" : fbdo.errorReason().c_str());
    json.clear();
    if (writeOk) {
      firebaseFailCount    = 0;
      firebaseSkipRemaining = 0;
      drainOnePendingReading();
      checkThresholdAlert();
      return true;
    } else {
      firebaseFailCount++;
      firebaseSkipRemaining = min(1u << firebaseFailCount, 16u);
      enqueuePendingReading(temperature, (uint32_t)timestamp);
      Serial.printf("[Firebase] Write failed (fail #%u), backing off %u cycles\n",
                    firebaseFailCount, firebaseSkipRemaining);
      return false;
    }
  } else {
    Serial.println("Invalid temperature and/or invalid timestamp.");
    return false;
  }
  countDataSentToFireBase += 1;
}
```

NOTA: `countDataSentToFireBase += 1` deve ser mantido — mover para antes dos returns onde fizer sentido (apenas em writes reais ou sempre). Manter o comportamento atual: incrementar apenas quando temperature/timestamp são válidos, independente do resultado. Reorganizar o fluxo para garantir o incremento.

**Ponto 2 — callFirebase(): adicionar skip guard no topo da função e final safety reset.**

Adicionar no início de callFirebase(), ANTES de `prevTemperature = temperature;`:

```cpp
// Backoff guard: pular ciclo se ainda em período de espera (per D-03)
if (firebaseSkipRemaining > 0) {
  firebaseSkipRemaining--;
  // Preservar leitura do ciclo skipped
  float currentTemp = readTemperature();
  if (currentTemp != 888.0 && currentTemp > -126.0) {
    uint32_t currentTs = (uint32_t)getTime();
    if (currentTs > MIN_VALID_TIMESTAMP) {
      enqueuePendingReading(currentTemp, currentTs);
    }
  }
  Serial.printf("[Firebase] Backoff: %u cycles remaining\n", firebaseSkipRemaining);
  return;
}

// Final safety cap: se max failures atingido, resetar (per D-04)
if (firebaseFailCount >= 5) {
  Serial.println("[Firebase] Max failures reached. Resetting device.");
  drawResetDevice();
  delay(2000);
  ESP.reset();
}
```

**Ponto 3 — setup(): chamar loadPendingQueue() após EEPROM.begin().**

Localizar a linha `EEPROM.begin(EEPROM_SIZE);` em setup() (~linha 416).
Inserir imediatamente após:

```cpp
loadPendingQueue();  // Restaurar leituras pendentes da EEPROM (per D-09)
```

**Ponto 4 — loop(): adicionar CONFIG_FETCH_INTERVAL timer (per D-15).**

Adicionar em loop() após o bloco `if (millis() - lastHeartbeatMillis > HEARTBEAT_INTERVAL)`:

```cpp
// Config fetch separado do ciclo de temperatura (per D-15/D-16)
if (millis() - configFetchPrevMillis > CONFIG_FETCH_INTERVAL || configFetchPrevMillis == 0) {
  configFetchPrevMillis = millis();
  getDeviceConfigurations();
}
```

NOTA sobre first-run: `|| configFetchPrevMillis == 0` faz o primeiro fetch ocorrer imediatamente no primeiro loop(). A primeira execução em setup() (~linha 522) permanece intacta (per D-16) — esse check em loop() é ADICIONAL para recargas periódicas, não substitui o setup() call.

Verificar que getDeviceConfigurations() NÃO aparece mais dentro de sendDataToFireBase() após as edições.
</action>
<verify>
<automated>cd /Users/manoel/repo/guardiao-project/guardiao-firmware && pio run -e nodemcuv2 2>&1 | tail -20</automated>
</verify>
<done>Build compila sem erros. grep confirma: "getDeviceConfigurations" não aparece em sendDataToFireBase(). "firebaseSkipRemaining" aparece em callFirebase(). "loadPendingQueue" aparece em setup(). "configFetchPrevMillis" aparece em loop().</done>
</task>

</tasks>

<verification>
```bash
cd /Users/manoel/repo/guardiao-project/guardiao-firmware
pio run -e nodemcuv2 2>&1 | grep -E "error|warning|SUCCESS|FAILED|RAM|Flash"
grep -n "getDeviceConfigurations" src/main.cpp
grep -n "firebaseSkipRemaining\|firebaseFailCount\|loadPendingQueue\|configFetchPrevMillis" src/main.cpp
grep -n "enqueuePendingReading\|drainOnePendingReading\|PendingReading" src/EEPROMFunction.h
```

Expected: BUILD SUCCESS, getDeviceConfigurations não em sendDataToFireBase, todas as funções da fila presentes.
</verification>

<success_criteria>

- Build compila sem erros (pio run -e nodemcuv2 exits 0)
- RAM usage permanece abaixo de 60KB (fila EEPROM usa ~130B RAM para pendingQueue[16])
- sendDataToFireBase() removeu getDeviceConfigurations() e captura bool do updateNode
- callFirebase() tem backoff guard no topo com skip cycle enqueue
- setup() chama loadPendingQueue() após EEPROM.begin()
- loop() tem CONFIG_FETCH_INTERVAL timer para getDeviceConfigurations()
  </success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-firebase-resilience-observability/02-01-SUMMARY.md`
</output>
